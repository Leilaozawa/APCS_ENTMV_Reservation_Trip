import { View, Text } from 'react-native';

const PaymentForm = () => {
    return (
        <View>
            <Text>This is the payment form.</Text>
        </View>
    );
};

export default PaymentForm;
