import { View, Text } from 'react-native';

const PaiementForm = () => {
    return (
        <View>
            <Text>This is the payment form.</Text>
        </View>
    );
};

export default PaiementForm;
